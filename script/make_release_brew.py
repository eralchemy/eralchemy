from poet.poet import formula_for
from subproces import Popen, PIPE
from script.m
import os

def main():
    formula = formula_for('eralchemy')
    Popen(['brew', 'update'])
    os.chdir('/usr/local')
    Popen(['git', 'checkout', '-b', 'er'])
